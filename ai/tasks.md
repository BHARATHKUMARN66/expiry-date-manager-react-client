# Implementation Tasks

## 1. UI Setup

* [x] Add Tailwind CSS, use #74b9ff as the primary color, #81ecec as the secondary color, and use other matching colors as and when needed.
* [x] Create landing page with header, footer, hero section, and put CTA to login/register**

* Header (logo, login/register links)
* Hero section (heading, sub-heading, CTA to login/register)
* Footer

## 2. Authentication & Session Management

* [ ] Build interactive Login and Registration Forms with validations.
* [ ] Integrate forms with backend APIs (`POST /auth/register` and `POST /auth/login`).
* [ ] Implement secure session persistence (saving user state and JWT).
* [ ] Redirect authenticated users to a placeholder Dashboard.

